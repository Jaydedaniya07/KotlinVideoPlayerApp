<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Base.Theme.JD.VideoApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Edge-to-Edge -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
        <!--<item name="android:windowLightNavigationBar">true</item>-->
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <!-- Use full gradient background -->
        <item name="android:windowBackground">@drawable/bg_theme_gradient</item>
        <!-- Ripple -->
        <item name="android:colorControlHighlight">#E5E7EB</item>
    </style>

    <style name="Theme.JD.VideoApp" parent="Base.Theme.JD.VideoApp" />

</resources>